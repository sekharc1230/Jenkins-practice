pipeline {
  agent { label 'javaapp' }
  stages
     stage('vcs') {
      steps {
        git url: https://github.com/sekharc1230/Jenkins-practice.git
      }
     }
     stage('package') {
      steps {
        sh 'mvn package'
      }
     }
}
